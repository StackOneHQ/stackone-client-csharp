//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace StackOne.Client.Models.Components
{
    using Newtonsoft.Json;
    using StackOne.Client.Utils;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;

    /// <summary>
    /// The type of the questions.
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class QuestionTypeValue : IEquatable<QuestionTypeValue>
    {
        public static readonly QuestionTypeValue ShortText = new QuestionTypeValue("short_text");
        public static readonly QuestionTypeValue LongText = new QuestionTypeValue("long_text");
        public static readonly QuestionTypeValue Attachment = new QuestionTypeValue("attachment");
        public static readonly QuestionTypeValue MultiSelect = new QuestionTypeValue("multi_select");
        public static readonly QuestionTypeValue SingleSelect = new QuestionTypeValue("single_select");
        public static readonly QuestionTypeValue Boolean = new QuestionTypeValue("boolean");
        public static readonly QuestionTypeValue Number = new QuestionTypeValue("number");
        public static readonly QuestionTypeValue Date = new QuestionTypeValue("date");
        public static readonly QuestionTypeValue Video = new QuestionTypeValue("video");
        public static readonly QuestionTypeValue ReferenceCheck = new QuestionTypeValue("reference_check");
        public static readonly QuestionTypeValue Url = new QuestionTypeValue("url");
        public static readonly QuestionTypeValue UnmappedValue = new QuestionTypeValue("unmapped_value");

        private static readonly Dictionary <string, QuestionTypeValue> _knownValues =
            new Dictionary <string, QuestionTypeValue> ()
            {
                ["short_text"] = ShortText,
                ["long_text"] = LongText,
                ["attachment"] = Attachment,
                ["multi_select"] = MultiSelect,
                ["single_select"] = SingleSelect,
                ["boolean"] = Boolean,
                ["number"] = Number,
                ["date"] = Date,
                ["video"] = Video,
                ["reference_check"] = ReferenceCheck,
                ["url"] = Url,
                ["unmapped_value"] = UnmappedValue
            };

        private static readonly ConcurrentDictionary<string, QuestionTypeValue> _values =
            new ConcurrentDictionary<string, QuestionTypeValue>(_knownValues);

        private QuestionTypeValue(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static QuestionTypeValue Of(string value)
        {
            return _values.GetOrAdd(value, _ => new QuestionTypeValue(value));
        }

        public static implicit operator QuestionTypeValue(string value) => Of(value);
        public static implicit operator string(QuestionTypeValue questiontypevalue) => questiontypevalue.Value;

        public static QuestionTypeValue[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as QuestionTypeValue);

        public bool Equals(QuestionTypeValue? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }

}