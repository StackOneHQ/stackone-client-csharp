//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace StackOne.Client.Models.Components
{
    using Newtonsoft.Json;
    using StackOne.Client.Utils;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;

    /// <summary>
    /// The unified message type.
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class CreateMessageValue : IEquatable<CreateMessageValue>
    {
        public static readonly CreateMessageValue Email = new CreateMessageValue("email");
        public static readonly CreateMessageValue Sms = new CreateMessageValue("sms");
        public static readonly CreateMessageValue Push = new CreateMessageValue("push");
        public static readonly CreateMessageValue WebPush = new CreateMessageValue("web_push");
        public static readonly CreateMessageValue IosPush = new CreateMessageValue("ios_push");
        public static readonly CreateMessageValue AndroidPush = new CreateMessageValue("android_push");
        public static readonly CreateMessageValue AppPush = new CreateMessageValue("app_push");
        public static readonly CreateMessageValue OmniChannel = new CreateMessageValue("omni_channel");
        public static readonly CreateMessageValue ContentBlock = new CreateMessageValue("content_block");
        public static readonly CreateMessageValue InApp = new CreateMessageValue("in_app");
        public static readonly CreateMessageValue Unknown = new CreateMessageValue("unknown");
        public static readonly CreateMessageValue UnmappedValue = new CreateMessageValue("unmapped_value");

        private static readonly Dictionary <string, CreateMessageValue> _knownValues =
            new Dictionary <string, CreateMessageValue> ()
            {
                ["email"] = Email,
                ["sms"] = Sms,
                ["push"] = Push,
                ["web_push"] = WebPush,
                ["ios_push"] = IosPush,
                ["android_push"] = AndroidPush,
                ["app_push"] = AppPush,
                ["omni_channel"] = OmniChannel,
                ["content_block"] = ContentBlock,
                ["in_app"] = InApp,
                ["unknown"] = Unknown,
                ["unmapped_value"] = UnmappedValue
            };

        private static readonly ConcurrentDictionary<string, CreateMessageValue> _values =
            new ConcurrentDictionary<string, CreateMessageValue>(_knownValues);

        private CreateMessageValue(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static CreateMessageValue Of(string value)
        {
            return _values.GetOrAdd(value, _ => new CreateMessageValue(value));
        }

        public static implicit operator CreateMessageValue(string value) => Of(value);
        public static implicit operator string(CreateMessageValue createmessagevalue) => createmessagevalue.Value;

        public static CreateMessageValue[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as CreateMessageValue);

        public bool Equals(CreateMessageValue? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }

}